import sh from'./Header.module.css';

const Header = () => {
    return(
        <header className={sh.header}>
            <i className="fas fa-basketball-ball"></i>
        </header>
    )
}

export default Header;
